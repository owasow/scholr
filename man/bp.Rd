% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inline-helpers.R
\name{bp}
\alias{bp}
\title{Format Coefficient and P-value Together}
\usage{
bp(model, var, b_digits = 2, p_digits = 3)
}
\arguments{
\item{model}{A fitted model object}

\item{var}{Character string naming the coefficient}

\item{b_digits}{Digits for coefficient (default: 2)}

\item{p_digits}{Digits for p-value (default: 3)}
}
\value{
Character string in format "b = X.XX, p = Y.YYY" or "b = X.XX, p < .001"
}
\description{
Extracts both coefficient and p-value and formats them as a single string.
Useful for inline reporting in R Markdown.
}
\examples{
m <- glm(am ~ wt + hp, data = mtcars, family = binomial)
bp(m, "wt")
# Returns: "b = -8.08, p = 0.014"

# In R Markdown prose:
# "Weight significantly predicted transmission (`r bp(m, 'wt')`)."
# Renders as: "Weight significantly predicted transmission (b = -8.08, p = 0.014)."
}
