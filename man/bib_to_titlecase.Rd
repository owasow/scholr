% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/titlecase.R
\name{bib_to_titlecase}
\alias{bib_to_titlecase}
\title{Process BibTeX File with Title Case Conversion}
\usage{
bib_to_titlecase(
  input_file,
  output_file = NULL,
  clean_locations = TRUE,
  clean_abbreviations = TRUE,
  fields_to_process = c("Title", "Journal", "Publisher", "Booktitle")
)
}
\arguments{
\item{input_file}{Path to the input BibTeX file}

\item{output_file}{Path for the output file. If NULL (default), creates a
timestamped file in the same directory as input.}

\item{clean_locations}{Logical. If TRUE (default), standardizes location
names (e.g., "New York, NY" becomes "New York").}

\item{clean_abbreviations}{Logical. If TRUE (default), expands common
publisher abbreviations (e.g., "Pr" becomes "Press").}

\item{fields_to_process}{Character vector of BibTeX field names to process.
Defaults to c("Title", "Journal", "Publisher", "Booktitle").}
}
\value{
Invisibly returns the path to the output file
}
\description{
Reads a BibTeX file and converts Title, Journal, Publisher, and Booktitle
fields to title case following NYT Manual of Style conventions.
}
\examples{
\dontrun{
# Process a BibTeX file
bib_to_titlecase("references.bib")

# Specify output file
bib_to_titlecase("references.bib", "references_cleaned.bib")

# Process only Title fields
bib_to_titlecase("references.bib", fields_to_process = "Title")
}

}
